# _*_ coding:utf-8 _*_
greeting = 'hello'
print greeting[0]
print greeting[-1]
''' 字符串就是一个由字符组成的序列。索引0指向第一个元素。从0开始计数的原因是也可以从最后一个元素开始计数:
序列中的最后一个元素标记为-1，倒数第二个元素为-2，意味着我们可以从第一个元素向前或向后计数'''
print 'hello'[1]
fourth = raw_input('Year:')[3]
print fourth
raw_input("press <enter>")
# 字符串字面值也能顾直接使用索引，而不需要一个变量引用他们
