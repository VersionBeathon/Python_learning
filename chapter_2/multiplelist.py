# _*_ coding:utf-8 _*_
# 用数字x乘以一个序列会生成新的序列，而在新的序列中，原来的序列将重复x次
print 'Python' * 5
print [42] * 10
# None、空列表和初始化
# None是一个Python的内建值，它的确切含意是“这里什么都没有”。因此想初始化一个长度为10的空表，可以按照如下：
sequence = [None] * 10 
print sequence

